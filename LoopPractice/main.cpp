#include <iostream>
#include <string>
#include <cctype>

//int sum(int a, int b);
//void draw_checkerboard(int size);

int main() {

    double x;
    x = 1 / 2;
    std::cout << x << std::endl;

    //std::cout << "enter a cherboard size: " << std::endl;
    //int size;
    //std::cin >> size;

    //draw_checkerboard(size);


    /* std::cout << "How many pizzas would you like? " << std::endl;
     int numPizzas;
     std::cin >> numPizzas;


     for (int i = 0; i < numPizzas; i++){
         std::cout << "For pizza " << std::endl;
     }
 */
    //int a;
    //if (a == 1 || a == 2 || a == 3)

    //std::cout << "Enter a size: " << std::flush;
    //std::string size;
    //std::cin >> size;

    //if (size == "small") {
    //std::cout << "The cost of a small pizza is $10" << std::endl;
    //}
//
//   int input = 0;
//   while (input < 10) {
//       std::cout << "Enter a number 10 or grater: ";
//       std::cin >> input;
//
//   }
//std::cout << sum(2, 5) << std::endl;

    return 0;
}


/*void draw_checkerboard(int size) {
    for (int row = 0; row < size; row++) {
        for (int col = 0; col < size; col++) {
            if (row % 2 == 0 && col % 2 == 0) {
                std::cout << "*";
            } else if (row % 2 != 0 && col % 2 != 0) {
                std::cout << "*";
            } else {
                std::cout << " ";
            }
        }
        std::cout << "\n";
    }

}
//int sum(int a, int b) {
//int sum = a+b;
//return sum;
//}*/
/*for (int row = 0; row < size; ++row) {
for (int col = 0; col < size; ++col) {
if (row == col){
std::cout << "*";
}else {
std::cout << " ";
}
}
std:: cout << "\n";
}*/